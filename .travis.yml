language: node_js
node_js:
  - "10.13.0"

jobs:
  include:
    - stage: Build
      script: yarn build
    - stage: Deploy
      deploy:
        provider: pages
        skip-cleanup: true
        github-token: $GITHUB_TOKEN
        keep-history: true
        target-branch: master
        on:
          branch: develop
